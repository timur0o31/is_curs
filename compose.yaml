services:
  postgres:
    image: postgres:latest
    environment:
      POSTGRES_DB: is_curs
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - is_curs_pgdata:/var/lib/postgresql/data

volumes:
  is_curs_pgdata:
